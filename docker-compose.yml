version: '2'
services:
  mysql:
    image: mysql
    container_name: mysql_databases
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=123456
    volumes:
      - ./mysql:/var/lib/mysql:rw
      - ./sqlfiles:/tmp/sqlfiles
